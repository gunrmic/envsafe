{
  "name": "envsafe",
  "version": "0.1.0",
  "description": "Encrypted, keychain-backed secret storage. Replaces .env files.",
  "type": "module",
  "bin": {
    "envsafe": "./dist/cli.js"
  },
  "keywords": ["env", "secrets", "security", "dotenv", "vault", "keychain"],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "test": "tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "picocolors": "^1.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0"
  }
}
